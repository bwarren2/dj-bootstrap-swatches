============
Installation
============

At the command line::

    $ easy_install dj-bootstrap-swatches

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dj-bootstrap-swatches
    $ pip install dj-bootstrap-swatches
